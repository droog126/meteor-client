# AutoWeb 得分计算逻辑（最新版）
bug:
3. 目前蜘蛛网的放置是非法的和岩浆放置不一样，岩浆放置是合法的
4. debug 也只渲染最终胜出的面
5. 是敌人在被网住时，会额外生成3个岩浆候选（上方 + 最近2个侧面），现在这个没好像整出来。


6. 不管有没有候选面，空桶对液体的放优先级永远是最高。然后水桶，对水的母体也是非常高的，超过候选面。 岩浆对水的母体和水的非母体和岩浆母体都是非常非常低的是负,但岩浆对非候选面和非水的和非岩浆的空气是有1的得分的，保证他不是负的。
7. 独立的水母体方块，不是含水方块始终保持渲染，即使不开debug模式，也会渲染。


8. 自己范围的内面，都应该是候选面才对，或者说根本没有候选面的概念，只是敌人的存在会对这个面有加成。


target Range不应该有，应该只能有target Range是敌人影响的范围，默认值应该为5。目前需要一个正在以自己为中心的range，这个要可以配置。一般来说15就可以了。所有要计算的面都在15格距离内。然后液体这个range去掉，其实液体的range其实和我自己的这个range很像。以这个逻辑重新

debug就是渲染出这个距离里所有的面的计算最终结果。懂吗？ 这个面是接触空气方块的那一面。这里说的面就是 range内，所有接触空气方块的面。 应该说是range*range*上下5高度即可。这个区域内所以后接触空气的面。

## 一、概述

AutoWeb 模块每 tick 执行一次完整扫描，为范围内所有目标生成候选放置点，并计算每个候选点的**优先级分数（priority）**。分数越高越优先处理。

模块同时支持四种操作：**蜘蛛网（Web）**、**岩浆（Lava）**、**空桶（Bucket）**、**水桶（Water Bucket）**，四者独立计算得分，最终选择**最高分**执行。

---

## 二、候选点生成规则

### 2.1 目标状态判定

对每个目标玩家，首先判定其状态：

| 状态 | 判定条件 | 首选类型 | 基础分数 |
|------|----------|----------|----------|
| **被网住** | 玩家站在蜘蛛网中 | LAVA | 100 |
| **着火** | `entity.isOnFire()` | WEB | 50 |
| **在空中** | `!isOnGround(entity)` | WEB | 60 |
| **在地面** | 其他情况 | LAVA | 50 |

### 2.2 库存强制覆盖

- **只有岩浆桶，没有蜘蛛网** → 所有目标强制生成 LAVA 候选点
- **只有蜘蛛网，没有岩浆桶** → 所有目标强制生成 WEB 候选点

### 2.3 候选点位置

每个目标生成 **脚部 3×3 共 9 个候选点**：

```
[目标位置为中心，Y层与脚部相同]
X-1,Z-1  X,Z-1  X+1,Z-1
X-1,Z    X,Z    X+1,Z
X-1,Z+1  X,Z+1  X+1,Z+1
```

### 2.4 主/次级候选点生成

根据首选类型和库存情况，生成主候选点和次级候选点：

#### 首选类型 = WEB 时：
- **WEB 主候选**：基础分数 = `priority`（空中时 `priority + 10`）
- **LAVA 次级候选**：基础分数 = `priority - 10`（空中）或 `priority / 2`（地面）

#### 首选类型 = LAVA 时：
- **LAVA 主候选**：基础分数 = `priority`
- **WEB 次级候选**：基础分数 = `priority + 10`（空中）或 `priority + 1`（地面）

#### 被网住时额外候选：
被网住时，额外在蜘蛛网上方和最近 2 个水平侧面生成 LAVA 候选点，分数 = `priority + 20`

---

## 三、候选点分数计算细则

### 3.1 基础分数公式

```
基础分数 = 状态基础分 + 距离加分

距离加分 = (targetRange - dist) * 2
```

- `targetRange`：扫描范围（默认 10）
- `dist`：玩家到目标的距离
- 距离加分最多约 +14（目标在脚下时）

### 3.2 岩浆（LAVA）候选点额外加分

#### 3.2.1 液体母体惩罚
如果目标位置本身是**岩浆母体或水母体**：
```
priority -= 9999
```

#### 3.2.2 周围无玩家加分
对岩浆候选点的每个面，检测其相邻位置周围 3×3 范围（同一 Y 层）是否存在其他玩家：
- **不存在玩家** → 该面 `+9` 分
- **存在玩家** → 该面 `+0` 分

检测范围：以面的相邻位置为中心，水平 3×3 共 9 格，同时检测玩家脚部位置和头部位置。

### 3.3 蜘蛛网（WEB）候选点额外加分

#### 3.3.1 可放置面加分
蜘蛛网候选点根据**可放置面的数量**额外加分：
```
priority += faces.size() * 5
```

### 3.4 空桶（Bucket）得分

- **条件**：瞄到母体液体 + 背包有空桶
- **得分**：**99999**（极高优先级）

### 3.5 水桶（Water Bucket）得分

- **条件**：瞄到水母体 + 背包有水桶
- **得分**：**100**（低于空桶的 99999）

---

## 四、完整分数计算流程

```
1. 扫描范围内所有目标玩家
   └── 对每个目标：
       ├── 判定状态（被网住/着火/空中/地面）
       ├── 确定首选类型和基础分数
       ├── 计算距离加分
       └── 生成候选点（主 + 次级）

2. 对每个候选点执行 tryAddCandidate：
   ├── 计算可放置面（faces）
   ├── 如果是 LAVA：
   │   ├── 液体母体惩罚（-9999）
   │   └── 周围无玩家加分（每个面无玩家 +9）
   ├── 如果是 WEB：
   │   └── 可放置面加分（每个面 +5）
   └── 加入 allCandidates 列表

3. 对 allCandidates 排序：
   └── 按 priority 降序，相同 priority 按距离玩家眼睛升序
```

---

## 五、执行逻辑（四者竞争）

每 tick 从四种操作中独立计算得分，选择最高分执行：

```
webScore   = 第一个可放置 WEB 候选点的 priority（没有则为 -1）
lavaScore  = 第一个可放置 LAVA 候选点的 priority（没有则为 -1）
bucketScore = 瞄到母体液体 ? 99999 : -1
waterBucketScore = 瞄到水母体 ? 100 : -1

maxScore = max(webScore, lavaScore, bucketScore, waterBucketScore)

if maxScore <= -1: 不执行任何操作

if bucketScore == maxScore:     执行空桶收液体
else if webScore == maxScore:   放置蜘蛛网
else if lavaScore == maxScore:  放置岩浆
else if waterBucketScore == maxScore: 放水换空桶
```

---

## 六、瞄准检测规则

### 6.1 Web/Lava 瞄准

玩家必须**直接瞄到候选点的可放置面**才算瞄准：
1. 射线检测命中某个方块的面
2. 检查该面是否是候选点的有效支撑面
3. **只有瞄到可放置面时**才返回该候选点

### 6.2 液体瞄准

射线检测命中液体方块即算瞄准，不限制具体面。

---

## 七、渲染规则

### 7.1 液体渲染

| 液体类型 | 条件 | 渲染行为 |
|----------|------|----------|
| **母体液体** | 周围1格没有其他母体（独立） | **始终渲染** |
| **母体液体** | 周围1格有其他母体（非独立） | 仅在 debug2 开启时渲染 |
| **非母体液体** | - | 仅在 debug2 开启时渲染 |

### 7.2 候选点渲染

- **debug（debug1）**：渲染可放置的候选面
- **debug2**：渲染不可放置的候选面（红色警告）
- 瞄到的候选点高亮显示（绿色）

---

## 八、分数示例

### 示例1：空中目标 + 近距离
- 状态：空中，基础分 60
- 距离：3 格，加分 (10-3)*2 = 14
- Web 主候选：60 + 14 + 10（空中加成）= 84
- 可放置面 3 个：+15
- **最终 Web 分数：99**

### 示例2：地面目标 + 岩浆母体惩罚
- 状态：地面，基础分 50
- 距离：5 格，加分 (10-5)*2 = 10
- Lava 主候选：50 + 10 = 60
- 目标位置是岩浆母体：-9999
- **最终 Lava 分数：-9939（不会执行）**

### 示例3：空桶瞄到水母体
- 条件：瞄到水母体 + 有空桶
- **空桶分数：99999（优先执行）**

---

## 九、注意事项

1. **分数为 -1 表示该操作不可执行**（没有对应物品或没有候选点）
2. **所有分数都为 -1 时，不执行任何操作**
3. **岩浆对岩浆母体和水母体都有 -9999 惩罚**
4. **蜘蛛网只有瞄到可放置面时才会放置**
5. **独立的母体液体方块始终渲染，不受 debug2 影响**
